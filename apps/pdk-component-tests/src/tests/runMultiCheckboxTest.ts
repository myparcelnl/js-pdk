import {ComponentTest, runCommonComponentTests} from '../common';

export const runMultiCheckboxTest: ComponentTest = (component) => {
  runCommonComponentTests(component);

  // TODO: write more tests
};
