import {ComponentTest} from '../types';
import {runHasSlotTest} from '../common';

export const runLinkTest: ComponentTest = (component) => {
  runHasSlotTest(component);

  // TODO write more tests
};
