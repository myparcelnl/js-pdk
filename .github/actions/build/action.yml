name: 'Create build'
description: 'Creates a build'

runs:
  using: composite
  steps:
    - name: 'Build packages'
      shell: bash
      #language=bash
      run: |
        NX_BRANCH=production yarn nx run-many \
          --runner=cloud \
          --parallel \
          --target=build \
          --output-style=static
