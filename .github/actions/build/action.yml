name: 'Create build'
description: 'Sets up environment and creates a build'

runs:
  using: composite
  steps:
    - name: 'Build packages'
      shell: bash
      run: |
        NX_BRANCH=production yarn nx run-many \
          --runner=cloud \
          --parallel \
          --target=build \
          --output-style=static
