{
  "name": "@myparcel-pdk/frontend-shared",
  "version": "1.0.0-alpha.0",
  "license": "MIT",
  "type": "module",
  "main": "lib/index.js",
  "module": "src/index.ts",
  "types": "lib/index.d.ts",
  "files": [
    "lib",
    "src"
  ],
  "scripts": {
    "build": "run ws:tsup:build \"$(pwd)\"",
    "build:dev": "run ws:tsup:build:dev \"$(pwd)\"",
    "buildOnly": "run ws:tsup:buildOnly \"$(pwd)\"",
    "clean": "run ws:clean \"$(pwd)\"",
    "lint": "run ws:lint \"$(pwd)\"",
    "serve": "run ws:tsup:serve \"$(pwd)\"",
    "watch": "run ws:tsup:watch \"$(pwd)\""
  },
  "eslintConfig": {
    "parserOptions": {
      "project": "tsconfig.build.json"
    }
  },
  "dependencies": {
    "@myparcel-pdk/common": "workspace:*",
    "@myparcel/ts-utils": "^1.2.0",
    "pinia": "^2.0.27",
    "vue": "^3.2.45"
  },
  "devDependencies": {
    "@myparcel-pdk/build-tsup": "workspace:*",
    "@myparcel/sdk": "^2.9.1",
    "tsup": "^6.5.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
