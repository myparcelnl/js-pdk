import {defineForm} from '@myparcel/vue-form-builder';

export const createLabelSettingsForm = () => {
  // const store = useContextStore();

  return defineForm('labelSettings', {
    fields: [],
  });
};
