{
  "$schema": "./node_modules/nx/schemas/nx-schema.json",
  "workspaceLayout": {
    "appsDir": "apps",
    "libsDir": "libs"
  },
  "defaultProject": "@myparcel/pdk-components",
  "npmScope": "myparcel-pdk",
  "tasksRunnerOptions": {
    "default": {
      "runner": "nx/tasks-runners/default",
      "options": {
        "parallel": 10,
        "cacheableOperations": ["build", "build:dev", "lint"]
      }
    }
  },
  "namedInputs": {
    "default": [
      "!{projectRoot}/lib",
      "{projectRoot}/**/*",
      "{workspaceRoot}/package.json",
      "{workspaceRoot}/tsconfig.build.json"
    ],
    "production": [
      "default",
      "!{projectRoot}/**/*.spec.*",
      "!{projectRoot}/**/*.test.*",
      "!{projectRoot}/**/__tests__"
    ],
    "src": ["{projectRoot}/src/**/*"]
  },
  "targetDefaults": {
    "lint": {
      "inputs": ["src"],
      "outputs": ["{projectRoot}/.eslintcache", "{projectRoot}/src/**/*"]
    },
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["production"],
      "outputs": ["{projectRoot}/lib", "{projectRoot}/dist"]
    },
    "build:dev": {
      "dependsOn": ["^build:dev"],
      "inputs": ["production"],
      "outputs": ["{projectRoot}/lib", "{projectRoot}/dist"]
    },
    "serve": {
      "dependsOn": ["build:dev"],
      "outputs": ["{projectRoot}/lib", "{projectRoot}/dist"]
    },
    "watch": {
      "dependsOn": ["^build:dev"],
      "inputs": ["production"],
      "outputs": ["{projectRoot}/lib", "{projectRoot}/dist"]
    }
  },
  "defaultBase": "main"
}
